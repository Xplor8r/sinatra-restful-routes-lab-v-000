<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8">
    <title>title</title>
  </head>
  <body>
    <h1></h1>
    <div class="container">
      <h1>Recipes</h1>

      <% @recipes.each.with_index do |recipe, index| %>
        <h2>Name: <%= @recipe[:name][index] %></h2>
        <p>Ingredients: <%= @recipe[:ingredients][index] %>
        <br>
        Cook time: <%= @recipe[:cook_time][index] %>
        </p>
        <form action="/recipes/<%= @recipes.id %>/delete" method="post">
          <input id="hidden" type="hidden" name="_method" value="delete">
          <input type="submit" value="delete">
        </form>
      <% end %>

      </form>
    </div>
  </body>
</html>
